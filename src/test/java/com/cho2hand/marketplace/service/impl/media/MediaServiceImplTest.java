package com.cho2hand.marketplace.service.impl.media;
import com.cho2hand.marketplace.entity.listing.Listing; import com.cho2hand.marketplace.exception.InvalidMediaException; import com.cho2hand.marketplace.repository.listing.ListingRepository; import com.cho2hand.marketplace.repository.media.*; import com.cho2hand.marketplace.storage.MinioProperties; import io.minio.MinioClient; import java.util.Optional; import org.junit.jupiter.api.Test; import org.springframework.mock.web.MockMultipartFile; import static org.junit.jupiter.api.Assertions.assertThrows; import static org.mockito.Mockito.*;
class MediaServiceImplTest { @Test void rejectsUnsupportedImageType(){var listings=mock(ListingRepository.class);var listing=new Listing();listing.setSellerUserId(1L);when(listings.findById(2L)).thenReturn(Optional.of(listing));var service=new MediaServiceImpl(listings,mock(MediaAssetRepository.class),mock(ListingImageRepository.class),mock(MinioClient.class),new MinioProperties("http://localhost:9000","key","secret","bucket"));assertThrows(InvalidMediaException.class,()->service.upload(1L,2L,new MockMultipartFile("file","x.txt","text/plain","x".getBytes())));}}
