package com.cho2hand.marketplace.service.chat; import com.cho2hand.marketplace.dto.chat.*; import java.util.List; public interface ChatService { Long open(Long buyerId,Long listingId); List<ConversationResponse> conversations(Long userId); MessageResponse send(Long userId,Long conversationId,SendMessageRequest request); List<MessageResponse> messages(Long userId,Long conversationId); }
